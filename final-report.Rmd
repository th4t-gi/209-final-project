---
title: "Final Project"
author: "Judd Brau, Alex Cyphers, Nathan Starhill"
date: "`r Sys.Date()`"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning=FALSE)

library(ggplot2)
library(dplyr)
theme_set(theme_bw())
```


# Final Report 


```{r, include=FALSE}
data <- read.csv("./data-complete.csv")

data <- data %>% mutate(time = as.POSIXct(time_of_day, format = "%H:%M:%S %p"),
                        type = if_else(card_entry == "N/A", "cash", "card"),
                        tip = if_else(card_tip == "N/A", -1, as.double(card_tip)),
                        tip_proportion = tip/total_money)
only_card_data <- data %>% filter(type == "card")

head(data)
```

